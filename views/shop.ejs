<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scatch</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-4">
    <h1 class="text-3xl font-bold text-start mb-6">Scatch</h1>
    
    <div class="flex gap-6 ">
        <!-- Sidebar -->
  <!-- Sidebar -->
<div class="w-1/4 bg-white p-4 rounded-lg shadow-md h-full min-h-screen flex flex-col">
  <h2 class="text-lg font-semibold mb-4">Sort By</h2>
  <ul class="space-y-2">
      <li><a href="#" class="block text-gray-700 hover:text-blue-600">Popular</a></li>
      <li><a href="#" class="block text-gray-700 hover:text-blue-600">New Collection</a></li>
      <li><a href="#" class="block text-gray-700 hover:text-blue-600">All Products</a></li>
      <li><a href="#" class="block text-gray-700 hover:text-blue-600">Discounted Products</a></li>
  </ul>

  <!-- Add spacing here -->
  <h2 class="text-lg font-semibold mt-8">Filter By</h2>
  <ul class="space-y-2 mt-2">
      <li><a href="#" class="block text-gray-700 hover:text-blue-600">Availability</a></li>
      <li><a href="#" class="block text-gray-700 hover:text-blue-600">Discount</a></li>
  </ul>
</div>

        
        <!-- Products Section -->
       <!-- Products Section (Ensure it stays at the top) -->
<div class="w-3/4 grid grid-cols-3 gap-4 items-start">
  <% products.forEach(function(product){ %>

      <div class="w-60 rounded-lg shadow-md text-start flex flex-col">
          <!-- Image Section -->
          <div class="w-full p-1 h-52 bg-[<%= product.bgcolor%>]">
              <img src="data:image/jpeg;base64,<%= product.image.toString('base64')%>" class="w-full h-[10rem] object-cover">
          </div>

          <!-- Content Section -->
          <div class="bg-[<%= product.panelcolor%>] text-[<%= product.textcolor%>] p-1 px-2 py-2 flex flex-col">
              <h3 class="text-sm font-semibold"><%= product.name%></h3>

              <div class="flex justify-between items-center mt-2">
                  <p>₹ <%= product.price%></p>
                  <button class="bg-white text-zinc-800 px-3 py-1 flex justify-center items-center rounded-full hover:bg-zinc-300">+</button>
              </div>
          </div>
      </div>

  <% }) %>
</div>

      
    </div>
</body>
</html>
